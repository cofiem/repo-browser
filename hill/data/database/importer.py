from hill.data.apt.item.control import Control


class Importer:
    def run(
        self,
        releases: list[Control],
        packages: list[Control],
        contents: list[Control],
        translations: list[Control],
    ):
        pass
